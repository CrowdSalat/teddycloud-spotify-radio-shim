# Changelog

All notable changes to this project will be documented in this file.

## [unreleased]

### Features

- *(telemetry)* Add pipeline rate sampler: chunk/drop/delivery counters

### Bug Fixes

- *(ocp)* Point ExternalSecret at Infisical store, match cluster template
- *(container)* Fully qualify base image names for multi-arch builds
- *(stream)* batch /stream writes into ≥128 KiB segments so ffmpeg drains the WAV stream at ~real time
- *(audio)* capture at 22050 Hz to halve the stream byte rate under the consumer's read ceiling

### Build

- Add OCI source label for GHCR repo linking
## [0.1.1] - 2026-09-10

### Documentation

- *(structure)* Mark phases 9 and 11 implemented

### Miscellaneous

- Bump golangci-lint-action to v7 for golangci-lint v2
- Update changelog for v0.1.1
## [0.1.0] - 2026-09-10

### Features

- Add project skeleton with Go module and Containerfile
- Add subprocess lifecycle manager for go-librespot
- *(audio)* Add FIFO reader with backpressure-safe discard
- *(server)* Add /stream endpoint and go-librespot API client
- *(server)* Add hot-swap and WebSocket event synchronisation
- Update go-librespot to 0.7.4
- Add run-local make command
- Start over as librespot is killed of by spotify. Try to use spotify soloist
- Phase 1 — Go module, config, HTTP server, ProcessManager, Containerfile
- Phase 2a — Soloist binary management (find, download, smoke-test)
- *(soloist)* Auto-pairing gate (phase 2b)
- *(soloist)* Connect-mode supervisor and WebSocket (phase 2c)
- *(shim)* Phase 3a — PulseAudio daemon with virtual sink
- *(shim)* Phase 3b.1 — core PCM chunk recorder
- *(shim)* Phase 3b.2 — live PulseAudio monitor recorder
- *(shim)* Phase 3b.3 — auto-reconnect monitor recorder
- *(shim)* Phase 4 — /stream WAV endpoint with soloist play
- *(shim)* Phase 5 — teddycloud SSE listener and mock
- *(mock)* Align SSE events with real Teddycloud
- *(sselistener)* Map real Teddycloud events
- *(scripts)* Add SSE capture helper for Teddycloud
- *(ci)* Add GHCR push targets and private-image pull docs
- *(server)* Hot-swap replaces active stream on new request
- *(deploy)* Add OpenShift manifests and session migration script

### Bug Fixes

- Name of container registry
- *(coordinator)* Only set lastURI on play success; extend pause timeout
- *(container)* Use golang:1.26, drop go.sum, split build/push targets

### Refactor

- *(librespot)* Consolidate packages and clarify file names
- Changed struct and variable names, no logic changes
- Consolidate audio and server package to radio package, no logic changes
- No logic changes only some variables and comments
- *(radio)* Simplify playback to backpressure-based model
- Point module path and image references at crowdsalat account

### Documentation

- Add project documentation and architectural decisions
- *(architecture)* Document go-librespot runtime findings
- Add TODO for OGG API non-radio Tonie content evaluation
- Add TEST.md with auth setup and integration test instructions
- Status update
- *(soloist)* Record Blocker 1 result - --single-track is track-only
- Rewrite DESIGN.md post-blocker; add STRUCTURE.md
- *(design)* Document AudioDaemon and ChunkSource interfaces for PipeWire portability
- *(design)* Note why UBI 9 is ruled out as base image
- *(structure)* Wire AudioDaemon and ChunkSource interfaces into phases 1–5
- Move ChunkSource to internal/audio; interface belongs to consumer
- *(structure)* Split Soloist into phases 2a/2b/2c; move PulseAudio to phase 3
- *(structure)* Split phase 3 into 3a (PulseAudio + sink) and 3b (recorder)
- *(plan)* Split Phase 3b, use parec over ffmpeg fallback
- *(plan)* Split Phase 8 into verifiable subtasks
- *(structure)* Reorder phases 6-7 for real-teddycloud validation
- *(research)* Capture real Teddycloud SSE event formats
- *(structure)* Add comment audit subtask to polish phase
- *(research)* Fix ear-press event order to match capture
- *(structure)* Prioritize GHCR image and session migration phases
- *(structure)* Finalize 8.2 tag-driven release, add 8.4 changelog task
- *(structure)* Mark phases 1-7 complete, phase 8 partial

### Testing

- Add integration test script for phases 1, 3, and 4
- *(blocker2)* Verify PulseAudio null-sink audio capture

### Build

- *(soloist)* Add make targets for local testing
- *(soloist)* Drop separate cache dir, keep it under /data
- *(soloist)* Keep cache in container, drop PODMAN override

### Miscellaneous

- *(makefile)* Improve lint and test output visibility
- Update to go 1.26.4
- Update go to 1.26
- Remove container ephemeral data that was checked in
- Remove blocker test Containerfiles and scripts
- *(makefile)* Rename image to teddycloud-spotify-shim
- *(lint)* Migrate golangci config to v2
- Add tag-driven GHCR build/publish workflow
- Add git-cliff changelog generation
- Update changelog fo v0.1.0
<!-- generated by git-cliff -->
